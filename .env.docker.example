# Docker Compose Environment Variables
# Source this file before running docker compose: source .env.docker
# Copy from your apps/api/.env or set these secrets manually

# Required Secrets (copy from apps/api/.env)
AUTH_TOTP_SECRET=your-totp-secret-here
AUTH_JWT_SECRET=your-jwt-secret-at-least-32-characters-long
OWNER_USER_ID=your-user-id-here

# Optional: Bypass code for local development (allows auth without TOTP setup)
# Default is "000000" - use this code to authenticate in local Docker environment
# Leave empty or unset in production
AUTH_BYPASS_CODE=000000
